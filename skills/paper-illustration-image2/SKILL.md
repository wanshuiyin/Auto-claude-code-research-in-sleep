---
name: paper-illustration-image2
description: "(Superseded in the Cursor pack) Paper illustration via the codex-image2 MCP backend. In this zero-API pack, use paper-illustration instead — it renders with Cursor's native GenerateImage tool."
argument-hint: "[description-or-method-file]"
---
> **ARIS-Cursor port — superseded stub.**
>
> Upstream, this variant existed as an alternative image-rendering backend
> (`mcp__codex-image2__generate_start` — Codex-native image generation) for
> users without a Gemini API key.
>
> In this Cursor pack that niche is covered natively:
> [`paper-illustration`](../paper-illustration/SKILL.md) renders with Cursor's
> built-in **GenerateImage** tool — zero keys, same supervised refinement loop.
>
> **→ Load `skills/paper-illustration/SKILL.md` and run it instead.**
>
> For precision vector diagrams use `/figure-spec` (deterministic SVG) or
> `/mermaid-diagram`. The upstream original lives at
> `$ARIS_REPO/skills/paper-illustration-image2/SKILL.md`.
